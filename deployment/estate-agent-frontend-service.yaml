apiVersion: v1
kind: Service
metadata:
  name: estate-agent-frontend
  namespace: default
spec:
  selector:
    app: estate-agent-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
