apiVersion: v1
kind: Service
metadata:
  name: estate-agent-frontend-service
  labels:
    app: estate-agent
    tier: frontend
spec:
  selector:
    app: estate-agent
    tier: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort

